spring:
  application:
    name: fastc-shop-ext

server:
  port: 8081

logging:
  config: classpath:logback.xml

springfox:
  documentation:
    swagger-ui:
      enabled: true

fastbe:
  login:
    color:
      exclude: /swagger**/**,/webjars/**,/v3/**,/doc.html


jsf:
  consumer:
    b2b:
      aurora:
        alias: test:0.0.1
  registry:
    index: test.i.jsf.jd.local